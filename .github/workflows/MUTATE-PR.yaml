---
name: Update Pull Request title
on:
  pull_request:
    types:
      - opened
jobs:
  update-pr:
    uses: ixxeL-DevOps/gha-templates/.github/workflows/__update-PR-classic__.yaml@v0.19.0-rc.0
    secrets:
      git-token: ${{ secrets.PAT }}
